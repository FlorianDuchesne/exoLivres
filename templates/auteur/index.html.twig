{% extends 'base.html.twig' %}

{% block title %}Auteurs{% endblock %}

{% block contenu %}
<table class="table table-striped table-hover table-sm">
    <thead>
        <tr>
            <th>NOM</th>
            <th>DATE NAISSANCE</th>
            <th>NATIONALITÉ</th>
            <th>MODIFIER</th>
            <th>SUPPRIMER</th>
        </tr>
    <thead>
    <tbody>
    {% for auteur in auteurs %}
    <tr>
        <td>
        <a href=" {{ path('auteur_show', {'id':auteur.id}) }}">
        {{ auteur.prenom ~ ' ' ~ auteur.nom }}
        </a>
        </td>
        <td>{{ auteur.dateNaissance | date('d/m/Y') }}</td>
        <td>{{ auteur.nationalite }}</td>
        <td><a class="btn btn-warning rounded-pill" href="{{ path('auteur_edit', {'id':auteur.id}) }}">Modifier</a></td>
        <td><a class="btn btn-danger rounded-pill" href="{{ path('auteur_delete', {'id':auteur.id}) }}">Supprimer</a></td> 
    </tr>
    {% endfor %}
    </tbody>
</table>
<a class="btn btn-success rounded-pill" href=" {{ path('auteur_add')}}">Ajouter un auteur</a>

{% endblock %}